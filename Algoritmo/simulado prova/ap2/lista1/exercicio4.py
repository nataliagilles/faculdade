def raiz (numero):
    inteiro= 0
    for valor in x:
        if valor in numero:
            s += valor

        else:
            inteiro = int(s)
            inteiro = inteiro**(1/2)
            inteiro= 0
        return resultado

n= int(input("Digite um número para calcular a raíz:"))
print(raiz(n))
            
